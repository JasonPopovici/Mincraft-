  
                
player.onChat("teleport", function() {
    
    agent.teleportToPlayer()
agent.move(FORWARD, 50) })

player.onChat("come", function () {
agent.turn(TurnDirection.Left)
agent.turn(TurnDirection.Left)

agent.move(FORWARD, 50) })
